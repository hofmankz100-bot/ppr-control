DATABASE_URL=postgresql://ppr_user:CHANGE_PASSWORD@localhost:5432/ppr_control
PGSSL=disable
PG_POOL_SIZE=10

# Для Render / Railway / Neon значение DATABASE_URL выдаёт сама платформа.
# Для облачной базы обычно оставьте PGSSL включённым:
# PGSSL=require
