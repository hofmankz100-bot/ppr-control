PPR Контроль v83

Что исправлено:
- архив из Downloads ppr-pwa-v81 (1).zip проверен: он совпадал с нашей v81;
- старая логика директорской сохранена: сотрудник пишет прямо в полном шаблоне служебной записки;
- директор видит полную служебку и может печатать;
- убраны лишние сохранения при простом просмотре директорской;
- убрано лишнее сохранение при простом построении списка заявок;
- сохранение на сервер теперь не запускается несколькими одновременными запросами;
- API-запросы не кэшируются service worker, чтобы интернет-версия не показывала старые данные и меньше лагала.

Локальная версия:
http://localhost:8080/?v=83

Render:
https://ppr-control-ramazan.onrender.com
