ППР Контроль v104 проверено

Что внутри:
- server.js: синхронизация через быстрый канал /api/events, без обязательной установки ws
- package.json: без dependencies, Render не должен делать npm install
- render.yaml: Build Command пустой, Start Command node server.js
- data/db.json: чистая база для старта
- app.js / app 2.js: текущая версия интерфейса v104-stable

Для GitHub:
1. Загрузить все файлы из этой папки в репозиторий ppr-control.
2. Удалить package-lock.json из GitHub.
3. Удалить db.json из корня GitHub.
4. Проверить, что есть папка data и внутри data/db.json.

Для Render:
Build Command оставить пустым.
Start Command: node server.js или npm start.
