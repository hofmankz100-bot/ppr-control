services:
  - type: web
    name: ppr-control
    env: node
    plan: starter
    buildCommand: "npm install"
    startCommand: "node server.js"
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATA_DIR
        value: /var/data
    disk:
      name: ppr-control-data
      mountPath: /var/data
      sizeGB: 1
